<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="DayTodayTransactions.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:DayTodayTransactions"
    Shell.FlyoutBehavior="Flyout"
     xmlns:pages="clr-namespace:DayTodayTransactions.Pages"
    Title="DayTodayTransactions">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate pages:TransactionHistoryPage}"
        Route="TransactionHistoryPage" />

    <!--<TabBar>
         --><!--Tab for TransactionHistoryPage--><!--
        <ShellContent Title="Transaction History" Route="TransactionHistoryPage" ContentTemplate="{DataTemplate pages:TransactionHistoryPage}">
            
        </ShellContent>
    </TabBar>-->
    
    <!-- Flyout Items -->
    <!--<FlyoutItem Title="Home" Icon="home.svg" FlyoutDisplayOptions="AsSingleItem" Route="home">
        <ShellContent Route="HomeMainPage" ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>


    <FlyoutItem Title="Sound Settings" Icon="sound_settings.svg" FlyoutDisplayOptions="AsSingleItem">
        <ShellContent ContentTemplate="{DataTemplate local:SoundSettingsView}" Route="SoundSettingsView" />
    </FlyoutItem>-->
</Shell>
